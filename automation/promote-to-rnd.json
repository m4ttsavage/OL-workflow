{
  "name": "VDSD promote to RND",
  "description": "When a VDSD issue is transitioned to Promote to Engineering, create an RND Feature or Task, copy labels/description, link implements, comment on VDSD.",
  "state": "ENABLED",
  "trigger": {
    "type": "jira.issue.event.trigger:transitioned",
    "value": { "to_status": ["Promote to Engineering"] }
  },
  "conditions": [{ "type": "jira.issues.scoped", "project": "VDSD" }],
  "actions": [
    {
      "type": "jira.issue.create",
      "project": "RND",
      "issuetype": "Task",
      "summary": "[{{issue.key}}] {{issue.summary}}",
      "copy": ["description", "priority", "labels"]
    },
    {
      "type": "jira.issue.link",
      "linkType": "Polaris work item link",
      "inward": "{{issue.key}}",
      "outward": "{{createdIssue.key}}"
    },
    {
      "type": "jira.issue.comment",
      "issue": "{{issue.key}}",
      "body": "Promoted to engineering {{createdIssue.key}}."
    }
  ],
  "import": "Project settings → Automation → Create rule. Recreate these steps in the UI if JSON import is rejected. Prefer scripts/promote_request.py so Feature vs Task and custom fields are copied."
}
